import sys
input = sys.stdin.readline

def sol() :
    N, M = list(map(int, input().split()))
    board = list(map(lambda _: list(map(str, input().strip())), range(N)))
    
    dirs = [(0, 1), (1, 0), (0, -1), (-1, 0)]
    
    def next(row: int, col: int, dirInd: int) :
        
        
    for row in range(N):
        for col in range(M):
            pass;    
    return False

print(sol())
    